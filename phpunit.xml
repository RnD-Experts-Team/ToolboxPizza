<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <!-- NOT sync: PublishOutboxEventJob does real NATS I/O, so a sync queue
             would make every write test try to reach a broker. The outbox row
             is the assertion target; publication is tested on its own. -->
        <env name="QUEUE_CONNECTION" value="null"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- Keep NATS subjects on their production names so tests assert the
             real strings rather than the *.testing.* twins. -->
        <env name="DEV_MODE" value="0"/>
        <!-- The breaks module records a milestone crossing either way; this
             only decides whether an outbox row is written for it. Off here so
             the milestone tests assert crossings without asserting events;
             BreakNotifierTest flips it on for itself. -->
        <env name="TOOLBOX_NOTIFICATIONS_ENABLED" value="false"/>
        <!-- Live break state over NotificationsPizza's Reverb. Off here too, so
             the break tests assert domain behaviour without also asserting
             wire traffic; BreakBroadcastTest flips it on for itself. -->
        <env name="TOOLBOX_REALTIME_ENABLED" value="false"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
